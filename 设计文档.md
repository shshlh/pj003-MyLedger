# 个人记账APP 设计与开发总文档

> 设计文档 + 开发路线图 + 工作链规范 — 一份文档驱动整个项目。
> 最后更新：2026-07-14

---

## 目录

1. [项目概述](#一项目概述)
2. [架构设计](#二架构设计)
3. [当前进度与路线图](#三当前进度与路线图)
4. [功能切片详解](#四功能切片详解)
   - 切片 1：基础记账 MVP ✅
   - 切片 2：账户体系闭环 ✅
   - 切片 3：投资中心
   - 切片 4：统计升级
   - 切片 5：自动化与同步
5. [数据模型](#五数据模型)
6. [开发工作链](#六开发工作链)
7. [技术栈与目录结构](#七技术栈与目录结构)
8. [附录](#八附录)

---

## 一、项目概述

### 1.1 背景

这是一款个人记账 App，目标是替代随手记。随手记的核心问题是**投资与日常收支数据混在一起**——买基金被记成「支出」，基金涨了被记成「收入」，月度报表完全失真。此外还有清仓账户删不掉、信用卡只能按自然月统计、无周期账单等问题。

### 1.2 核心差异化

| 随手记痛点 | 根本原因 | 本方案 |
|-----------|---------|--------|
| 基金盈亏混入收支报表 | 数据库无投资隔离机制 | `is_investment` 标记字段，投资数据独立统计 |
| 清仓账户无法删除 | 无归档状态设计 | `status` 字段：active / archived / deleted |
| 信用卡按自然月统计 | 无账单周期概念 | `billing_day` + `repayment_day` 字段 |
| 多账户转账需逐笔记 | UI 只支持单笔操作 | 链式转账编辑器 |
| 固定支出需手动记 | 无周期账单功能 | `periodic_bills` 表 + 自动生成引擎 |

### 1.3 设计原则

- **功能切片优先于技术分层**：每个迭代交付一个独立可用的完整体验，不以「先把所有 model 写完」为目标
- **先数据模型后 UI**：表结构设计一次到位（参考随手记踩过的坑），UI 只是数据的消费者
- **本地单设备优先**：先做好本地 SQLite，多设备同步是最后再考虑的事
- **投资标记而非分表**：用 `is_investment` 字段区分交易类型，不建独立的投资交易表，保持 schema 简洁

---

## 二、架构设计

### 2.1 账本容器架构

随手记最值得借鉴的设计——「以账本为顶层容器」。用户进入一个账本后，所有记账、统计、账户、分类都在该账本下工作：

- **数据天然隔离**：家庭账本和生意账本互不干扰
- **切换方便**：在账本列表快速切换工作空间
- **代码结构一早就支持多账本**：即使目前只需一个，以后扩展不需要重构

```
┌────────────────────────────────────────────┐
│              个人记账APP                     │
│                                              │
│  ┌─────────┐  ┌──────────────────────────┐  │
│  │ 账本列表 │  │      当前账本工作区        │  │
│  │ ──────  │  │  ┌────┬────┬────┬────┐  │  │
│  │ 我的账本◄│  │  │记账│账户│投资│报表│  │  │
│  └─────────┘  │  └────┴────┴────┴────┘  │  │
│               │      │                    │  │
│               │  ┌───┴────────┐           │  │
│               │  │ SQLite 数据库│          │  │
│               │  └────────────┘           │  │
│               └──────────────────────────┘  │
└────────────────────────────────────────────┘
```

### 2.2 模块职责

| 模块 | 文件 | 职责 |
|------|------|------|
| 入口 + 初始化 | `main.dart` | App 启动、数据库初始化、默认账本/账户/分类创建 |
| 首页 | `pages/home_page.dart` | 月收支概览、近期交易、周期账单检查、各 tab 导航 |
| 记账 | `pages/record_page.dart` | 选分类→填金额→选账户→保存，记账后实时刷新首页 |
| 流水 | `pages/transaction_list_page.dart` | 交易记录列表，支持下钻查看 |
| 统计 | `pages/statistics_page.dart` | 月度分类占比 |
| 数据库 | `database/database_helper.dart` | 所有 SQLite 操作：CRUD、记账事务、统计查询、周期账单引擎 |
| 模型 | `models/*.dart` | Book、Account、Category、Transaction、PeriodicBill |

### 2.3 数据流

```
用户操作（记账页）
    ↓
DatabaseHelper.recordTransaction()
    ├── INSERT INTO transactions
    ├── UPDATE accounts SET balance（在事务中）
    └── （投资标记 is_investment 决定该记录进入日常报表 or 投资报表）
    ↓
HomePage / 统计页 → 查询 transactions（按 is_investment 过滤）→ 渲染
```

---

## 三、当前进度与路线图

### 3.1 完成状态一览

**✅ 已完成（切片 1 — 基础记账 MVP）**

| 功能 | 文件 | 状态 |
|------|------|------|
| 项目骨架 + Flutter 配置 | `pubspec.yaml`, `main.dart` | ✅ |
| Book 模型 + 默认账本初始化 | `models/book.dart`, `database_helper.dart` | ✅ |
| Account 模型 + CRUD + 归档 | `models/account.dart` | ✅ |
| Category 模型 + 默认分类 | `models/category.dart` | ✅ |
| Transaction 模型（含 `is_investment`） | `models/transaction.dart` | ✅ |
| 记账逻辑（支出/收入/转账/invest） | `database_helper.dart` 的 `recordTransaction()` | ✅ |
| 账户余额事务一致性 | 同上，所有余额操作在一个事务中 | ✅ |
| PeriodicBill 模型 + 到期检查 + 自动生成 | `models/periodic_bill.dart`, `runDueBills()` | ✅ |
| 月度收支汇总查询 | `getMonthlySummary()` | ✅ |
| 分类汇总查询（供统计图表） | `getCategorySummary()` | ✅ |
| 首页（概览 + Tab 导航 + 刷新机制） | `pages/home_page.dart` | ✅ |
| 记账页（类型选择 + 自定义金额键盘 + 账户分类选择） | `pages/record_page.dart` | ✅ |
| 流水页（交易列表） | `pages/transaction_list_page.dart` | ✅ |
| 统计页（分类占比列表） | `pages/statistics_page.dart` | ✅ |
| `dart analyze` 无错误 | — | ✅ |

**❌ 未完成**

| 功能 | 所属切片 | 优先级 |
|------|---------|--------|
| 投资持仓模型 + 买入/卖出/清仓流程 | 切片 3 | 🔴 高 |
| 投资盈亏独立报表 | 切片 3 | 🔴 高 |
| fl_chart 图表（饼图/柱状图/折线图） | 切片 4 | 🟡 中 |
| 多维度统计筛选 | 切片 4 | 🟡 中 |
| 净资产总览 | 切片 4 | 🟢 低 |
| 净值联网获取 | 切片 5 | 🟢 低 |
| CSV 导入/导出 | 切片 5 | 🟢 低 |
| WebDAV 同步 | 切片 5 | 🟢 低 |
| 单元测试 + Widget 测试 | 各切片 | 🔴 高 |

### 3.2 路线图总览

```
切片 1：基础记账 MVP          ████████████ ✅ 已完成（2026-07-14）
切片 2：账户体系闭环          ████████████ ✅ 已完成（2026-07-16）
切片 3：投资中心              ░░░░░░░░░░░░ 🔜 当前（约 5-7 天）
切片 4：统计升级              ░░░░░░░░░░░░ （约 3-5 天）
切片 5：自动化与同步          ░░░░░░░░░░░░ （约 5-7 天）
```

## 四、功能切片详解

### 切片 1：基础记账 MVP ✅ 已完成

> **目标**：能创建账本→配置分类→创建账户→记账→看统计，形成闭环。

**已实现功能**：见上文完成状态表。

**v0.2 经验沉淀**：
- IndexedStack + initState 导致数据过期 → 所有子页面暴露 `refresh()` 方法
- 私有 State 类 + GlobalKey 跨文件不可见 → 需要跨文件引用的 State 类不加 `_` 前缀
- 模板测试不匹配实际 App → 项目初始化后立刻替换为 smoke test
- 每个页面覆盖加载中/空态/正常态/错误态

---

### 切片 2：账户体系闭环 ✅ 已完成

> **目标**：让「账户」不只是余额数字——账单周期、链式转账、账户管理形成完整闭环。
> **为什么放一起**：它们共同依赖 account model，改动集中在 home_page 和 record_page，不波及统计模块。

#### 2.1 信用卡账单周期

**实现要点**：

- 在 `DatabaseHelper` 中新增 `getBillingCycle(Account creditCard, DateTime date)` 方法
- 入参：信用卡账户（含 billing_day 和 repayment_day）+ 任意日期
- 出参：该日期对应的账单周期起止日期
- 规则：
  ```
  账单日每月5日，还款日每月25日
  7月3日消费 → 本期：6月5日 ~ 7月5日，还款日：7月25日
  7月6日消费 → 本期：7月5日 ~ 8月5日，还款日：8月25日
  ```
- 首页新增「信用卡」区域：显示本期已用额度、应还金额、距离还款日天数

**边界测试清单**：

- [ ] 消费日期 = 账单日当天（决定：账单日当天归属本期结束日，即归属本期）
- [ ] 跨年账单周期（12月 → 1月）
- [ ] 月末账单日（31日 → 2月只有28/29天，需取 min(账单日, 当月最大天数)）
- [ ] 非信用卡账户（billing_day 为空）不触发账单周期查询

#### 2.2 链式转账

**实现要点**：

- 在 `record_page.dart` 中新增「链式转账」模式
- 数据结构：
  ```dart
  class TransferNode {
    final String fromAccountId;
    final String toAccountId;
    final double amount;
  }
  ```
- 校验规则：
  - 金额守恒：所有节点的 amount 之和 = 总流出 = 总流入
  - 同一 from 账户的所有转出之和 ≤ 该账户余额
  - 至少包含一个节点（单节点等价于普通转账，应允许）
- UI 交互：
  - 列表形式展示转账节点，每行：from → to, 金额
  - 底部「+」按钮添加节点
  - 提交时在一个事务中执行所有转账

**边界测试清单**：

- [ ] 空链（0 个节点）→ 拒绝
- [ ] 单节点 → 等价于普通转账，应允许
- [ ] 金额不守恒 → 明确报错并标出差异金额
- [ ] 转出总额超过余额 → 明确报错并标出哪个账户不足
- [ ] 自己转自己 → 应拒绝

#### 2.3 账户管理 UI

- 新建 `pages/accounts_page.dart`：账户列表（按类型分组）+ 新建/编辑入口
- 账户编辑：名称、类型、初始余额（仅新建时可设）、账单日/还款日（仅信用卡）
- 归档操作：余额 > 0 时弹窗提示并阻止；余额 = 0 时二次确认
- 已归档账户折叠到「已归档」分组，可恢复

**切片 2 验收标准**：

- 信用卡首页显示本期账单周期和消费金额
- 链式转账一键完成多条记录，每条金额正确，余额同步更新
- 账户列表支持分组展示 + 归档/恢复操作
- `dart analyze` 零错误

---

### 切片 3：投资中心 🔜 当前

> **目标**：能买入、跟踪、卖出、清仓基金/股票，盈亏独立统计，不影响日常收支报表。

#### 3.1 投资持仓模型

在 `models/` 下新增 `investment.dart`：

```dart
class InvestmentHolding {
  final String id;
  final String bookId;
  final String accountId;     // 关联的投资账户
  final String code;           // 基金/股票代码
  final String? name;          // 名称
  final String invType;        // fund / stock
  final double totalCost;      // 总成本（累计买入金额 - 累计卖出回款）
  final double totalShares;    // 当前持有份额
  final double? latestNav;     // 最新净值（手动输入 or 联网获取）
  final String? navDate;       // 净值日期
  final String feeType;        // A / B / C / custom（基金费率类型）
  final int isLiquidated;      // 0=持仓中, 1=已清仓
  final String createdAt;
  final String updatedAt;
}
```

**为什么只建一张表**：Kimi 路线图画了 `investments` + `investment_transactions` 两张表。我的判断是不需要第二张——买入/卖出操作直接走 `transactions` 表（`type='invest'`, `is_investment=1`），通过 `related_investment_id` 关联到持仓。这样统计时只需过滤 `is_investment=1`，不用跨表 join。

#### 3.2 买入流程

1. 用户输入：基金代码、买入金额、费率类型、买入日期、扣款账户、当日净值
2. 系统计算（参考 Kimi 的费率表）：
   - 申购费 = 买入金额 × 申购费率
   - 净申购金额 = 买入金额 - 申购费
   - 份额 = 净申购金额 / 当日净值
3. 写入：
   - `transactions`：一条 type=invest, is_investment=1 的记录
   - `investment_holdings`：新建 or 更新持仓（份额累加、成本累加）
   - `accounts`：扣款账户余额 -= 买入金额，投资账户余额 += 买入金额

#### 3.3 卖出与清仓

- 卖出部分份额：输入卖出份额 + 当日净值 → 计算到手金额 − 赎回费 → 更新持仓
- 清仓：卖出全部份额，标记 `is_liquidated=1`
- 清仓后：显示本次投资总盈亏 = 累计卖出回款 − 累计买入成本

#### 3.4 费率参考表（Kimi 贡献）

| 类型 | 申购费 | 赎回费 | 销售服务费 | 适用场景 |
|------|--------|--------|------------|----------|
| A类 | 1.5%（通常打一折 0.15%） | 持有越久越低 | 无 | 长期持有 |
| B类 | 无 | 持有越久越低，最终为 0 | 无 | 中长期 |
| C类 | 无 | 7天内 1.5%，之后 0 | 0.5%/年 | 短期 |
| 自定义 | 用户自行输入 | 用户自行输入 | 用户自行输入 | 特殊情况 |

> 用户可覆盖默认值，支持自定义费率。

#### 3.5 投资盈亏报表

- 新增 `pages/investment_page.dart`：持仓列表（名称/成本/市值/盈亏/收益率）
- 点击进入持仓详情页：交易历史、净值变化、盈亏计算
- 投资盈亏**不进入**首页的月度收支统计（通过 `is_investment=1` 过滤）

**切片 3 边界测试清单**：

- [ ] 买入后持仓份额、成本、账户余额正确
- [ ] 卖出部分份额后持仓更新正确，到账金额扣除了赎回费
- [ ] 清仓后 `is_liquidated=1`，盈亏计算 = 卖出总额 − 买入总额
- [ ] 卖出份额超过持仓 → 拒绝并提示
- [ ] 投资交易不出现在日常收支报表
- [ ] 清仓后投资账户余额归零

---

### 切片 4：统计升级

> **目标**：从「列表数字」升级到「可视化图表 + 多维筛选」。

#### 4.1 图表实现

| 图表 | 用途 | 库 |
|------|------|-----|
| 饼图 (PieChart) | 月度分类支出占比 | fl_chart |
| 柱状图 (BarChart) | 月度支出对比（近 6 个月） | fl_chart |
| 折线图 (LineChart) | 净资产趋势（总资产 − 总负债） | fl_chart |

在 `statistics_page.dart` 中增加图表 Tab，默认显示饼图，可切换。

#### 4.2 多维筛选

- 时间维度：日/周/月/季/年/自定义范围
- 分类维度：一级分类、展开二级分类（当前 categories 表有 parent_id，但未使用，此切片启用）
- 账户维度：单账户/多账户/全部
- 类型切换：仅支出 / 仅收入 / 收支对比

#### 4.3 净资产总览

```
总资产 = 资金账户余额总和 + 投资账户市值总和
总负债 = 信用卡已用额度总和
净资产 = 总资产 − 总负债
```

显示在首页顶部，替代当前仅显示月度收支的方式。

**切片 4 性能基准**：

- 月度统计查询（含图表数据）< 1 秒
- 1 万条交易记录时滑动不卡顿
- 图表渲染不阻塞 UI 线程

---

### 切片 5：自动化与同步

> **目标**：减少手动操作 + 多设备数据互通。**这些是「有更好」，不是「必须有」。**

#### 5.1 净值联网获取

- 方案：先做手动输入（切片 3 已支持），稳定后再探索免费 API
- 候选 API：天天基金、东方财富等公开接口
- 风险：反爬、接口变更，需要容错和降级到手动模式

#### 5.2 CSV 导入/导出

**导出**：统计页 → 导出按钮 → 生成 CSV（含日期/类型/金额/分类/账户/备注）→ 分享/保存

**导入**（支付宝/微信账单）：
```
用户选择 CSV 文件 → 解析 → 字段映射 → 智能匹配分类 → 预览确认 → 批量入账
```

**字段映射参考（Kimi 贡献）**：

| 支付宝字段 | App 字段 | 映射策略 |
|-----------|---------|---------|
| 交易时间 | datetime | 直接映射 |
| 收/支 | type | 「支出」→ expense，「收入」→ income |
| 金额 | amount | 取绝对值 |
| 交易对方 | note | 存入备注，供分类匹配参考 |
| 商品说明 | — | 用于自动匹配分类的关键词识别 |

#### 5.3 WebDAV 同步

```
记账 → 更新 SQLite → 记录变更 → 手动/自动上传到 WebDAV
换设备 → 检查 WebDAV 更新 → 下载 → 覆盖本地（时间戳比对）
```

- 设置页输入 WebDAV 地址/账号/密码
- 手动同步为主，自动同步为可选项
- 冲突策略：时间戳比对，新的覆盖旧的

**切片 5 性能基准**：

- CSV 导出 1000 条记录 < 2 秒
- 数据库备份 < 5 秒
- 启动时间（含周期账单检查）< 2 秒

---

## 五、数据模型

### 5.1 当前 Schema（已实现，与代码一致）

```sql
CREATE TABLE books (
    id TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    cover TEXT,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL
);

CREATE TABLE accounts (
    id TEXT PRIMARY KEY,
    book_id TEXT NOT NULL,
    name TEXT NOT NULL,
    type TEXT NOT NULL,            -- cash / debit / credit / fund / stock / virtual
    balance REAL NOT NULL DEFAULT 0,
    currency TEXT NOT NULL DEFAULT 'CNY',
    status TEXT NOT NULL DEFAULT 'active',  -- active / archived / deleted
    billing_day INTEGER,           -- 信用卡账单日（仅 credit 类型使用）
    repayment_day INTEGER,         -- 信用卡还款日
    sort_order INTEGER NOT NULL DEFAULT 0,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    FOREIGN KEY (book_id) REFERENCES books(id)
);

CREATE TABLE categories (
    id TEXT PRIMARY KEY,
    book_id TEXT NOT NULL,
    name TEXT NOT NULL,
    type TEXT NOT NULL,            -- expense / income
    parent_id TEXT,                -- 父级分类 ID（二级分类，切片 4 启用）
    icon TEXT,
    sort_order INTEGER NOT NULL DEFAULT 0,
    created_at TEXT NOT NULL,
    FOREIGN KEY (book_id) REFERENCES books(id)
);

CREATE TABLE transactions (
    id TEXT PRIMARY KEY,
    book_id TEXT NOT NULL,
    account_id TEXT NOT NULL,
    to_account_id TEXT,            -- 转账目标账户
    category_id TEXT,
    type TEXT NOT NULL,            -- expense / income / transfer / invest
    amount REAL NOT NULL,
    datetime TEXT NOT NULL,
    note TEXT,
    is_investment INTEGER NOT NULL DEFAULT 0,  -- ⭐ 关键字段：0=日常, 1=投资
    related_investment_id TEXT,    -- 关联的 investment_holdings.id
    created_at TEXT NOT NULL,
    FOREIGN KEY (book_id) REFERENCES books(id),
    FOREIGN KEY (account_id) REFERENCES accounts(id),
    FOREIGN KEY (category_id) REFERENCES categories(id)
);

CREATE TABLE periodic_bills (
    id TEXT PRIMARY KEY,
    book_id TEXT NOT NULL,
    name TEXT NOT NULL,
    type TEXT NOT NULL,            -- expense / income
    amount REAL NOT NULL,
    account_id TEXT NOT NULL,
    category_id TEXT,
    frequency TEXT NOT NULL,       -- daily / weekly / monthly / yearly / custom
    interval_days INTEGER,         -- 自定义间隔天数（frequency=custom 时使用）
    start_date TEXT NOT NULL,
    end_date TEXT,                 -- 空 = 永久
    next_run_date TEXT NOT NULL,
    enabled INTEGER NOT NULL DEFAULT 1,
    created_at TEXT NOT NULL,
    FOREIGN KEY (book_id) REFERENCES books(id),
    FOREIGN KEY (account_id) REFERENCES accounts(id)
);
```

### 5.2 待新增表（切片 3）

```sql
CREATE TABLE investment_holdings (
    id TEXT PRIMARY KEY,
    book_id TEXT NOT NULL,
    account_id TEXT NOT NULL,      -- 关联的投资账户
    code TEXT NOT NULL,            -- 基金/股票代码
    name TEXT,                     -- 名称
    inv_type TEXT NOT NULL,        -- fund / stock
    total_cost REAL DEFAULT 0,     -- 总成本
    total_shares REAL DEFAULT 0,   -- 当前持有份额
    latest_nav REAL,               -- 最新净值
    nav_date TEXT,                 -- 净值日期
    fee_type TEXT DEFAULT 'A',     -- A / B / C / custom
    is_liquidated INTEGER DEFAULT 0,
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    FOREIGN KEY (book_id) REFERENCES books(id),
    FOREIGN KEY (account_id) REFERENCES accounts(id)
);
```

### 5.3 余额变动规则

| 操作 | from 余额 | to 余额 | 统计影响 |
|------|----------|---------|---------|
| 支出 | −amount | — | 进入日常支出报表 |
| 收入 | — | +amount | 进入日常收入报表 |
| 转账 | −amount | +amount | 不影响收支统计 |
| 投资买入 | −amount（资金账户） | +amount（投资账户） | 进入投资报表 |
| 投资卖出 | +amount（资金账户） | −amount（投资账户） | 进入投资报表 |

> 所有余额操作在同一个 SQLite 事务中完成，保证一致性。详见 `DatabaseHelper.recordTransaction()`。

### 5.4 为什么 is_investment 用标记字段而非分表

随手记的教训：投资和收支混在一起是因为当初设计时没有预留隔离机制。Kimi 路线图建议分两张 `investment_transactions` 表，但这样：
- 投资买入/卖出需要同时写两张表 → 增加事务复杂度
- 统计时需要跨表 JOIN → 查询变慢
- 后续加新类型（如「应收应付」）又要加表

用一个 `is_investment` 标记更简洁：所有交易在一张表里，统计时按标记过滤即可。`related_investment_id` 只在需要追溯到具体持仓时使用。

---

## 六、开发工作链

### 6.1 分工策略

**主体模式：我亲自开发。** 架构决策、跨模块改动、UI 交互、代码审查、测试——全部由我直接执行。这对一个几千行 Dart 代码的项目来说是最高效的方式，保证风格一致、上下文连贯。

**加速模式：子 agent 做可隔离的机械性任务。** 符合以下条件时用 `codeagent-wrapper --parallel` 分发：

1. 任务有明确输入/输出规格（如「给 account.dart 和 category.dart 写单元测试，覆盖 toMap/fromMap/边界值」）
2. 不依赖运行时状态（不需要启动 App 验证）
3. 不与正在进行的其他改动冲突

**不使用的场景**：
- 涉及 UI 布局和交互的任务（需要视觉判断）
- 需要理解跨模块影响的架构改动
- 任何需要启动 Flutter 验证的任务

### 6.2 单切片工作流

```
1. 读：确认涉及哪些文件，读取当前代码
       ↓
2. 写：逐文件实现（apply_patch），改完一个验证一个
       ↓
3. 审：自查改动（对照审查清单）
       ↓
4. 分析：dart analyze lib\
       ↓
5. 测：运行相关单元/Widget 测试
       ↓
6. 提交：git commit（附改动摘要）
```

### 6.3 代码审查清单（每次提交前过一遍）

**Bug 级别**：
- [ ] async 操作后是否检查了 `mounted`（StatefulWidget 中）
- [ ] 数据库写操作是否在事务中
- [ ] 空值/空列表/边界金额是否有处理

**风险级别**：
- [ ] 新增的数据库查询是否加了 `is_investment=0` 过滤（日常报表）
- [ ] IndexedStack 子页面是否暴露了 `refresh()` 方法
- [ ] 新 model 类的 toMap/fromMap 是否与表结构一致

**风格级别**：
- [ ] 新增页面是否覆盖了加载中/空态/错误态
- [ ] 私有 State 类命名（需要跨文件引用时不加 `_`）
- [ ] 新增文件路径是否符合目录结构约定

### 6.4 测试策略

| 层级 | 范围 | 触发条件 |
|------|------|---------|
| 模型单元测试 | toMap/fromMap、边界值、序列化 | model 类新增或字段变更时 |
| 数据库单元测试 | 增删改查、事务回滚、统计查询 | DatabaseHelper 新增方法时 |
| 业务逻辑测试 | 费率计算、账单周期、余额变动规则 | 新增计算逻辑时 |
| Widget 测试 | 关键交互（记账按钮、金额输入） | 页面大改时 |
| 边界测试 | 按切片中的边界测试清单执行 | 切片完成时 |

> 测试不追求覆盖率数字（90% 对个人项目不现实），追求「所有边界条件和人容易犯错的路径都有测试」。

---

## 七、技术栈与目录结构

### 7.1 技术选型

| 类别 | 选择 | 说明 |
|------|------|------|
| 框架 | Flutter (Dart) | 跨平台，一套代码跑 Windows + Android |
| 本地数据库 | sqflite + sqflite_common_ffi | SQLite，Windows 用 ffi 模式 |
| 状态管理 | setState + GlobalKey | 当前规模够用，不加 Riverpod/BLoC |
| 图表 | fl_chart | 切片 4 引入 |
| UUID | uuid | 主键生成 |
| 日期格式化 | intl | DateFormat |
| 路由 | Navigator push/pop | 页面少，不需要 go_router |

### 7.2 实际目录结构

```
my_account_book/
├── lib/
│   ├── main.dart                    # 入口 + 初始化逻辑
│   ├── database/
│   │   └── database_helper.dart     # 所有 SQLite 操作
│   ├── models/
│   │   ├── account.dart
│   │   ├── book.dart
│   │   ├── category.dart
│   │   ├── periodic_bill.dart
│   │   ├── transaction.dart
│   │   └── investment.dart          # 切片 3 新增
│   ├── pages/
│   │   ├── home_page.dart           # 首页 + Tab 容器
│   │   ├── record_page.dart         # 记账页
│   │   ├── statistics_page.dart     # 统计页
│   │   ├── transaction_list_page.dart # 流水页
│   │   ├── accounts_page.dart       # 切片 2 新增
│   │   └── investment_page.dart     # 切片 3 新增
│   └── widgets/                     # 切片 2 起逐步拆出公共组件
│       └── amount_keyboard.dart     # （当前在 record_page 内）
├── test/
│   └── widget_test.dart
├── pubspec.yaml
└── web/                             # Web 构建产物
```

> 与 Kimi 路线图目录结构的差异：不加 `repositories/`、`services/`、`providers/` 层。5-10 个页面的项目不需要这些抽象层，数据库操作方法直接放在 `DatabaseHelper` 里，状态管理用 setState。原则是「不到疼的时候不抽象」。

---

## 八、附录

### A. 随手记痛点对照

| 痛点 | 随手记问题 | 本方案 |
|------|-----------|--------|
| 基金盈亏记起来麻烦 | 需手动每日填收益，盈亏混入收支 | `is_investment` 独立标记，盈亏独立统计 |
| 投资影响月度报表 | 没有数据隔离 | `is_investment=1` 不进入日常报表 |
| 信用卡按自然月 | 无账单周期概念 | `billing_day` + `repayment_day` 自定义周期 |
| 多账户转账繁琐 | A→B→C 需记多笔 | 链式转账一次性填写 |
| 清仓账户无法删除 | 只能禁用 | `status` 字段支持归档+删除 |
| 无周期账单 | 没这个功能 | `periodic_bills` 表 + 自动引擎 |

### B. 投资费率参考（Kimi 贡献）

见切片 3 的费率表。实际开发时作为默认值硬编码，用户可在买入时覆盖。

### C. 性能基准（Kimi 贡献，已做调整）

| 指标 | 目标 | 验证切片 |
|------|------|---------|
| 启动时间 | < 2 秒 | 切片 5 |
| 月度统计查询 | < 1 秒 | 切片 4 |
| 1 万条记录滑动 | 不卡顿 | 切片 4 |
| CSV 导出 1000 条 | < 2 秒 | 切片 5 |
| 数据库备份 | < 5 秒 | 切片 5 |

### D. v0.2 开发反馈（保留）

- **做好**：数据库层一次性设计到位、静态分析前置、代码审查在提交前、设计文档驱动开发、状态处理模式统一
- **反复出错**：IndexedStack 数据过期、私有 State + GlobalKey、模板测试不匹配、单 patch 改多文件
- **核心经验**：数据库先行、分析先行、审查先行——三层前置检查。多 tab 场景下数据新鲜度是系统性风险，需从架构层面解决（暴露 refresh）

---

> **一句话总结这个文档的意图**：不是让你一次读完，而是在每个切片开始前，翻到对应章节，看一眼目标 + 要点 + 边界测试清单，然后动手——写完对照审查清单自查——跑分析——跑测试——提交。循环往复。

### E. v0.3 开发反馈（2026-07-15）

**切片 2 踩坑记录**：

| 问题 | 根因 | 避免方式 |
|------|------|---------|
| 信用卡消费余额更新方向反了 | 统一用 balance - amount，但信用卡余额表示债务，消费应增加余额 | 账户类型感知：信用卡支出 balance + amount，其他 balance - amount。每次新增交易类型先想清楚余额含义 |
| 信用卡账单周期逻辑错误 | 只算了当前周期的在途消费，没考虑上期应还的概念 | 信用卡有两个概念：当前周期（在途，未出账单）和上期（已出账单，要还的）。两者分开计算 |
| 还款功能方向搞反 | 偿还信用卡时债务减少，但 transfer 统一 balance + amount | 转账到信用卡时做 balance - amount（债务减少）。所有 to-account 的余额变更都要先查账户类型 |
| 转账/链式转账崩溃 | _toAccountId 可能等于 _accountId，下拉列表过滤后列表为空 | 所有 _firstToAccountId() 调用加 exclude: _accountId 参数。链式节点 _addChainNode() 额外检查 toId != fromId |
| PowerShell 正则替换破坏文件结构 | 用 -replace 或 Regex.Replace 跨行匹配时，匹配边界不精确导致误伤 | 能用 apply_patch 就别用正则。需要大量替换时写 Python 脚本到文件再执行 |
| Git checkout 丢失未提交修改 | 为修复正则损坏的文件执行了 git checkout -- file，包含了全部未提交的工作 | git checkout -- file 前先用 git diff 确认这文件真有提交版本可回退 |
| DropdownButtonFormField.value 弃用 | Flutter 3.33+ 弃用了 value 参数 | 改用 initialValue 配合 ValueKey 强制重建 |
| apply_patch 不支持一个 patch 改多个文件 | patch 语法限制 | 一个 apply_patch 调用只改一个文件，多文件改动并行发多个调用 |
| 正则匹配了多个 }); | 想修复一个合并问题，结果匹配了文件中所有同样格式的行 | 修改前先确认匹配的唯一性，或使用更精确的上下文锚定 |

**v0.3 核心经验总结**：

1. 账户类型感知是系统性风险 - 余额变更逻辑不能一刀切。储蓄卡、信用卡、投资账户的余额含义不同 |
2. 信用卡不是普通账户 - 它有两个时间线（当前周期和已出账单），UI 上要分开展示在途和应还 |
3. 严格的分步工作流 - 每个功能切片有明确的验收标准。在验收通过之前，不继续下一项 |
4. 正则文件替换是最后手段 - 它不可逆、不可调试。apply_patch 更安全，Python 脚本次之 |
5. 子 agent 用得少 - 这个项目规模下，亲自开发比分发子任务更高效
